# CLAUDE.md

このリポジトリで作業する Claude 向けの引き継ぎメモ。

---

## 1. このプロジェクトは何か

TradingView の **Squeeze Momentum Indicator [LazyBear]** に、PerpDEX の perp 取引で
コストを超えるエッジが存在するかを検証したプロジェクト。

**目的は bot 化ではなく「エッジの有無を判定すること」だった。**
エッジが無ければそこで終了してよい、という前提で始まっている。

### 最終結論（2026-09 時点）

**エッジは確認できなかった。Squeeze / WaveTrend / ADX の 3 指標について検証は完全終了。**

依頼者の合意のもと、**この 3 指標について新しい切り口を試すことはしない**。

---

## 2. 絶対に守るルール

このプロジェクトは「検証の作法」を守ることに価値がある。
成績を良く見せることではない。

1. **Hold-out（2026-06-01 .. 2026-08-31）は未開封。** 最後の 1 枚として温存する。
   `data/cache/` に 2026-06 以降のファイルが存在しないことが封印の証拠。
2. **中止基準に到達したらパラメータを振り直さない。** これは skill 5.3 の
   データスヌーピングそのもの。
3. **結果を見てから仮説・候補・判定ホライズンを差し替えない。**
   選定ルールはグリッドを回す前にコミットする（実例: commit `e14014c`）。
4. **逆張り（符号反転）を後から足さない。** 試したいなら探索前にグリッドへ入れる。
5. **依頼者を安心させる方向に結論を寄せない。** エッジが無いという結論は
   成功した検証である。
6. **不利な数字も必ず並べる。** 良かったパラメータだけを報告しない。
7. **「勝率」を主要指標にしない。** 損益分布と期待値で判断する。
8. **業者ブログの数字（勝率 88〜92% 等）をベンチマークにしない。**
9. 作業前に `backtest-validation` skill を読む。

---

## 3. 開発環境の注意点

- **Binance REST API（`fapi.binance.com`）はこの実行環境から使えない**（HTTP 451、地域制限）。
  代わりに公開ダンプ **`data.binance.vision`** の月次アーカイブを使う
  （`data/fetch_binance_klines.py`）。内容は API の kline と同一。
- 依存パッケージ: `pandas numpy scipy tabulate`（`pip install` で都度入れる）
- **両取引所の公式ドキュメントは egress proxy で遮断されている**
  （`hyperliquid.gitbook.io`, `docs.lighter.xyz` とも `EGRESS_BLOCKED`）。
  `COSTS.md` の手数料は二次情報ベースの暫定値であり `# 未確認` と明記してある。
- `data/cache/` は `.gitignore` 済み。取得済みは
  **2023-01〜2024-12（15m/1h）** と **2025-01〜2026-05（5m/15m/1h）**。

---

## 4. これまでの経過

| フェーズ | 検証内容 | 結果 |
|---|---|---|
| Phase 0 | 指標実装と正確性検証 | テスト 28 項目合格・独立実装と一致 |
| Phase 1 | スクイーズ解放時の `val` の符号（既定パラメータ）| **0 / 18** |
| Phase 1 追試 | 解放定義を原典（黒→灰）に修正 | 完全に同一（青が構造的に 0 件）|
| Phase 1b | WaveTrend / ADX 単体（標準設定）| **0 / 36** |
| Phase 1c Part A | 65 設定 × 3 時間足 × 6 ホライズン = 1,170 セルの探索 | 候補 4 件を事前登録 |
| Phase 1c Part B | 未使用データ 2023-2024 で確認 | **0 / 4** |
| Phase 1c 安定性 | 四半期別の記述統計 | 4 候補すべて、最良 1 四半期を除くとコスト割れ |

### 各フェーズの要点

**Phase 0**: `stdev` は母標準偏差（`ddof=0`）、`rangema` は SMA(TR) であって
Wilder の ATR ではない、`linreg` は閉じた形で実装。KC 期間とモメンタム期間を
独立パラメータに分離。ルックアヘッド検査器は**陰性対照 6 種**で先に検査してある。

**Phase 1 追試の副産物**: 既定パラメータでは `bb_length == kc_length == 20` のため
BB と KC の中心線が一致し、原典の 2 条件が `dev < rangema*1.5` という 1 本の不等式に
帰着する。したがって青（`no_sqz`）は構造的に発生しない。
Phase 1c で BB 期間 ≠ KC 期間の設定を試したところ、実際に青が発生した（逆側からの裏付け）。

**Phase 1b**: EMA は `ewm(span=n, adjust=False)`、Wilder は `ewm(alpha=1/n, adjust=False)`。
Phase 0 の SMA(TR) とは別物。混同しないこと。EMA の立ち上がりを避けるため
**先頭 500 本を解析対象外**にしている。

**Phase 1c の族ごとの地形**（自分のスコアと隣接設定スコア中央値の Spearman 相関）:
ADX +0.66 / WaveTrend +0.79 は「平らな丘」、**Squeeze は +0.17 で「孤立した山ばかり」**。
探索**前**に測ったこの指標が、Part B で Squeeze 候補だけ符号反転した事実を予測していた。

**Phase 1c 安定性分析（重要）**: 未使用期間（Part B）で最良の 1 四半期を除くと、
**4 候補すべてが想定コスト 0.85 bps を下回る**（−2.42 / −16.28 / −1.16 / +0.54 bps）。
一方、探索に使った Part A は全四半期がコスト超え。選択バイアスの教科書どおりの姿。

---

## 5. コスト前提（`COSTS.md` に詳細）

| 項目 | 値 | 確度 |
|---|---|---|
| Lighter Maker / Taker（standard perp）| 0.000% | 二次情報・未確認 |
| Hyperliquid Maker / Taker（base tier）| 0.015% / 0.045% | 二次情報・未確認 |
| **依頼者の実測スリッページ** | **$10,000 で往復 $0.7〜1.0（= 0.7〜1.0 bps）** | **実測** |
| 判定に使った最良コスト（シナリオ F）| **1.0 bps** | — |

**実測スリッページの測定条件は揃っている。** 依頼者の lead-lag 戦略
（Binance ETHUSD の先行を見て Lighter BTCUSD でポジションを取る）は、
定義上「価格が大きく動いた瞬間」に発注するロジックであり、
本プロジェクトが想定する局面と同種である。
ただし測定は **Lighter の BTCUSD** に限られ、ETH / SOL は未測定。

**決定的な閾値: 2.33 bps。** 候補 4 の Part B 平均（コスト前）がこの値なので、
実測往復コストがこれを超えたら中心の期待値もマイナスになる。

---

## 6. リポジトリ構成

| ファイル | 内容 |
|---|---|
| `squeeze_momentum.py` | Squeeze Momentum の指標計算のみ（売買ロジックなし）|
| `main_indicators.py` | WaveTrend / ADX の指標計算のみ |
| `data/fetch_binance_klines.py` | kline 取得 + 健全性検査（理論本数照合・欠損の規則性）|
| `lookahead_check.py` | 切断法によるルックアヘッド検査 + 陰性対照 6 種 |
| `tests/reference_impl.py` | Pine から独立に書き起こした素朴なループ実装（検算用）|
| `tests/test_squeeze_momentum.py` | Phase 0 の正確性検証（28 項目）|
| `tests/test_main_indicators.py` | WaveTrend / ADX の正確性検証（30 項目）|
| `tests/test_phase1b.py` | Phase 1b の集計ロジックの検算（13 項目）|
| `phase1_event_study.py` / `phase1_run.py` | Phase 1 のイベントスタディ |
| `phase1_nearmiss.py` | 最良セルの精査 |
| `phase1_release_definition.py` | 解放定義（黒→灰 / 黒→青 / 黒→黒以外）の比較 |
| `phase1b_event_study.py` / `phase1b_run.py` | Phase 1b |
| `phase1c_grid.py` | グリッド定義と候補選定ルール（**実行前に確定・コミット**）|
| `phase1c_engine.py` | 評価エンジン（Part A / B 共通）|
| `phase1c_partA.py` | Part A: 探索 |
| `phase1c_partB_data.py` | Part B: データ取得と健全性検査 |
| `phase1c_partB.py` | Part B: 事前登録候補のみの検定 |
| `phase1c_stability.py` | 時間的な安定性の記述統計 |
| `COSTS.md` | コスト前提と出典 |
| `reports/*.md`, `reports/*.csv` | 各フェーズのレポート |

全テストの実行:
```bash
pip install pandas numpy scipy tabulate
python3 tests/test_squeeze_momentum.py   # 28 項目
python3 tests/test_main_indicators.py    # 30 項目
python3 tests/test_phase1b.py            # 13 項目
```

---

## 7. 次にやること: シャドーペーパートレード

### 背景

依頼者は当初 $1,000 のライブトレードで最新のスリッページを測ることを検討したが、
**実弾を使わずに同じものが得られる**ため、シャドーモードに方針を変更した。

Walk-Forward Analysis は**やらない**。
WFA はデータを分割するので判定に使える件数が減り、確信度はむしろ下がるため
（全期間 6,067 件で標準誤差 1.68 bps → OOS 5 割で 2.38 bps）。
確信度はデータ量で決まり、分析手法では増えない。

### 次のセッションの構成

**リポジトリ 2 つを指定して作業する**:
- `bot-build-Squeeze-Momentum`（このリポジトリ。シグナル生成ロジック）
- `bot-build-Lighter`（**Lighter のペーパートレード基盤が既にある**）

### シャドーモードとは

1. ボットを実際に稼働させ、シグナルはリアルタイムで判定する
2. **注文は一切出さない**。代わりにシグナル発生の瞬間に
   **Lighter の L2 orderbook スナップショットを保存**する
3. 後から「$1,000 の成行注文を出したら板をどこまで食ったか」を計算する（板を歩く）

得られるもの:
- 条件が揃ったスリッページの実測値（理論約定）
- バックテストとライブのシグナル・タイミングの突き合わせ（skill Section 4）
- **資金リスクゼロ**

得られないもの:
- レイテンシ、部分約定、キュー位置、自分の注文による板の変化
  （$1,000 のテイカー注文なら BTC では無視できる水準）

### 対象と規模

**候補 4: ADX length=14 / threshold=25 / 15分足 / 12 本保有（= 3 時間）**
（`reports/phase1c_registered_candidates.json` に事前登録済み）

- シグナル頻度: 週 **33.8 件**（BTC/ETH/SOL 合計、各銘柄 週 11 件強）
- 必要件数の目安: 100 件で 3 週、200 件で 6 週

### 実装時に必ず決めておくこと（走らせる前に）

**目的の明記**: これは「儲けるための運用」ではなく **「測定のための運用」**。
成功の定義は「スリッページ分布が分かった」「バックテストとライブが一致した」であり、
**「儲かった」ではない**。

**停止条件**（最初に来たもの）:
- スリッページ実測 **200 件**に到達
- **8 週間**経過

**判定ルール**:
- 実測往復コストが **2.33 bps を超えた** → 中心の期待値もマイナス。**この系統は完全に終了**
- 下回った → コスト前提が確定。ただし**エッジの証明にはならない**

**銘柄はプールしない**。Part B の内訳は BTC +3.92 / ETH +4.24 / **SOL −1.19 bps** で、
**SOL だけマイナス**。Lighter の SOL 板は BTC より薄いはずなので、
銘柄ごとに別々に記録する。

**記録項目**:
- シグナル時刻、その時点の ADX / DI+ / DI−（判断の再現に必要）
- 板スナップショット（best bid/ask、各段の数量、少なくとも $1,000 を食い切る深さまで）
- 板を歩いて計算した理論約定価格（エントリー / エグジット両方）
- **ファンディング**（3 時間保有なので発生しうる。
  Lighter の BTC/ETH/SOL のファンディングは `COSTS.md` の未解決事項）
- バックテストが同じ時刻に想定した価格（突き合わせ用）

### 実装で注意すること

- **判断はバー確定後、約定は次バーの始値**という分離をライブでも守る。
  15 分足なら、バーが閉じた時刻に判定し、次バーの最初のティックで約定と見なす。
- バックテストは `close_time`（バー確定時刻）を index にしている。ライブ側も揃える。
- ADX の Wilder 平滑化は逐次更新できる（`y[i] = x[i]/n + y[i-1]*(n-1)/n`）が、
  **立ち上がりを揃えるため最低 500 本の履歴を先に流し込んでから**判定を始める。
- ライブとバックテストで ADX 値が一致することを、最初に必ず突き合わせる。
  ここがズレていたら、以降の測定はすべて無意味になる。

---

## 8. やってはいけないこと（再掲）

- Hold-out（2026-06 .. 2026-08）を開ける
- 3 指標（Squeeze / WaveTrend / ADX）で新しい切り口を試す
- シャドーモードの結果を見てからパラメータや判定ルールを変える
- シャドーの損益（理論値）を見て「儲かりそう」と判断する
  — 1 トレードのばらつきは ±13 bps に対し期待値は +0.23 bps。**ノイズが 56 倍**
- 「動いているから」という理由でライブに移行する
